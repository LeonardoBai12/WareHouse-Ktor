name: Test coverage verification

on:
  pull_request:

jobs:
  coverage-verification:
    runs-on: macos-latest

    steps:
      - uses: actions/checkout@v2

      - name: Set up JDK 19
        uses: actions/setup-java@v2
        with:
          java-version: 19
          distribution: 'adopt'

      - name: Make gradlew executable
        run: chmod +x ./gradlew

      - name: Run tests
        run: |
          ./gradlew test
          echo "::set-output name=test_status::$?"

      - name: Generate Tests Report
        if: ${{ steps.coverage_verification.outputs.test_status == '0' }}
        uses: actions/upload-artifact@v3
        with:
          name: WareHouse-Test-Report
          path: 'build/reports/tests/test/'

      - name: Validate coverage
        id: coverage_verification
        run: |
          ./gradlew jacocoTestCoverageVerification
          echo "::set-output name=coverage_verification_status::$?"

      - name: Generate coverage report (If JaCoCo finds violations)
        if: ${{ steps.coverage_verification.outputs.coverage_verification_status == '0' }}
        run: ./gradlew jacocoTestReport

      - name: Upload Unit Tests Coverage Report (If JaCoCo finds violations)
        if: ${{ steps.coverage_verification.outputs.coverage_verification_status == '0' }}
        uses: actions/upload-artifact@v3
        with:
          name: WareHouse-Coverage-Report
          path: 'build/reports/jacoco/test/html/'